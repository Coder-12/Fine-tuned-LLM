model:
  base_model_name: "huggyllama/llama-2-7b"   # Replace with the desired model identifier
  lora_rank: 8
  learning_rate: 5e-5
  epochs: 3
  batch_size: 8

data:
  training_data_path: "data/finance_data.json"

training:
  output_dir: "outputs"
